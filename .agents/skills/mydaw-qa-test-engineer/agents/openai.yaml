interface:
  display_name: "MyDAW QA Test Engineer"
  short_description: "Tests workflows, component states, timing, reliability, and alpha readiness."
  default_prompt: "Use this skill to define or run MyDAW workflow acceptance tests, component-state checks, MIDI timing tests, scene switching tests, audio dropout tests, CPU stress tests, regressions, and alpha release validation."
